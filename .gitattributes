# Normalize line endings to LF in the repo, CRLF on Windows checkout
* text=auto eol=lf
# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.woff binary
model-rag/model.pkl filter=lfs diff=lfs merge=lfs -text
